# whisper.cpp/ggml (pulled in via whisper-rs, macOS/Linux only - see
# Cargo.toml) needs libc++ and the Accelerate framework linked on macOS.
[target.x86_64-apple-darwin]
rustflags = ["-lc++", "-lframework=Accelerate"]

[target.aarch64-apple-darwin]
rustflags = ["-lc++", "-lframework=Accelerate"]
